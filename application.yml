server:
  port: 33400

spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop

  kafka:
    bootstrap-servers: localhost:9092

general:
  kafka-topic: transactions-0
  incentive-api-url: http://localhost:33400
